# TraceKite demonstration corpus

The repositories and deployment files in this directory are small synthetic
fixtures authored for TraceKite. They demonstrate a configuration-derived HTTP
dependency between `orders-service` and `billing-service`; they are not copies
of production repositories.

These fixtures are distributed under the repository's Apache License 2.0.
`labels.yml` records the expected evidence used by the accuracy and release
checks.
